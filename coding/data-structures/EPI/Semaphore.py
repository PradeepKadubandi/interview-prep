# acquire : wait for permit, permit given only to 1 requestor at a time 
# release : release permit

def semaphore():
    def __init__(self):
        q = []
        locked = False

    def acquire():
        with lock:

